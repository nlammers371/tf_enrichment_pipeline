function snipCleanup(snipPath)
    
    filePattern = fullfile(snipPath, '*.mat'); % select all .mat files
    theFiles = dir(filePattern);
    if ~isempty(theFiles)
      disp(['Removing ' num2str(length(theFiles)) ' from temp snip director 
    for k = 1 : length(theFiles)
      baseFileName = theFiles(k).name;
      fullFileName = fullfile(myFolder, baseFileName);      
      delete(fullFileName);
    end